import XCTest

final class VoiceKeyboardUITests: XCTestCase {
    // UI テストは Phase12 の計画に基づき実装されますが、CI 環境でシミュレータが必要なためここではプレースホルダーとします。
    func testPlaceholder() {
        XCTAssertTrue(true)
    }
}
